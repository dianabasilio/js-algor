const str = "Hola";

// Obtener el valor Unicode del primer carácter (H)
const unicodeValue = str.charCodeAt(0);

console.log(
  `El valor Unicode del primer carácter "${str[0]}" en "${str}" es: ${unicodeValue}`
);
//El valor Unicode del primer carácter "H" en "Hola" es: 72
