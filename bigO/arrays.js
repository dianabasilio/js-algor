//When you need order

let array1 = ["Lola", "Carmen", "Luis"];

//access O(1)
console.log(array1[2]);

//Insertion IT DEPENDS
//push O(1) at the end of the array

//unshift O(n) because you have to reindex every element in the array

//Remove IT DEPENDS
//pop O(1) at the end of the array

//shift O(n) because you have to reindex every element in the arrays

//searching O(n)

//concat O(n) to "merge 2 arrays"

let array2 = ["Juan", 2];
console.log(array1.concat(array2));
//[ 'Lola', 'Carmen', 'Luis', 'Juan', 2 ]

//slice O(n)

//splice O(n)

//sort O(nlogn)
